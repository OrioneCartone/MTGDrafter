# --- Core Data Science & Machine Learning ---
# Pandas per la manipolazione dei dati, specialmente nei notebook
pandas>=2.0.0
# Numpy per operazioni numeriche, è una dipendenza di quasi tutto
numpy>=1.24.0
# Scikit-learn per future utility di ML (es. scaling, metriche)
scikit-learn>=1.3.0
# PyTorch, il nostro framework di deep learning principale
torch>=2.0.0
torchvision

# --- Data Collection & Web ---
# Per fare richieste HTTP alle API di Scryfall e CubeCobra
requests>=2.31.0
# Per mostrare barre di progresso durante i download
tqdm>=4.65.0

# --- Sviluppo e Notebooks ---
# Il motore per far funzionare i notebook Jupyter
jupyter>=1.0.0
# Ipywidgets per interattività futura nei notebook
ipywidgets>=8.0.0

# --- Utilities ---
# Per la gestione dei file di configurazione YAML
pyyaml>=6.0
# Per la gestione dei log
loguru>=0.7.0
